# Auth Feature

Handles all authentication logic including user login, session management, and route protection.

## Includes
- `AuthProvider` – manages auth state globally
- `LoginPage` – user login UI
- `ProtectedRoute` – restricts access to authenticated users only